[project]
name = "placement-intelligence-assistant"
version = "0.1.0"
description = "Placement Intelligence Assistant RAG API"
dependencies = []

[tool.fastapi]
entrypoint = "backend.main:app"

[tool.vercel]
entrypoint = "backend/main.py:app"
